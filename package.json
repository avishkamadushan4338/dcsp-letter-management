{
  "name": "letter-management-system",
  "version": "1.0.0",
  "description": "Divisional Correspondence Secretariat (DCS) letter tracking system - number issuing, minuting and officer action tracking via emailed links.",
  "private": true,
  "license": "UNLICENSED",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "bun alchemy dev --profile=admin ",
    "test": "bun run typecheck",
    "deploy": "NODE_ENV=production bun alchemy deploy --profile admin --stage=prod",
    "deploy:github": "NODE_ENV=production bun alchemy deploy stacks/github.ts --profile admin"
  },
    "devDependencies": {
      "alchemy": "next"
    },
  "dependencies": {
    "effect": ">=4.0.0-beta.97 || >=4.0.0",
    "@effect/platform-bun": ">=4.0.0-beta.97 || >=4.0.0",
    "@effect/platform-node": ">=4.0.0-beta.97 || >=4.0.0",
    "dotenv": "^17.4.2",
    "drizzle-orm": "^0.45.2",
    "nodemailer": "^9.0.3"
  }
}
